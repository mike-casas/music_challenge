<div class="container">

<div class="row">
  <% @artists.each do |artist| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <img src="<%= artist.image_url%>" alt=<%= artist.name %>>
        <div class="caption">
          <h3><%= artist.name %></h3>
          <p><%= link_to "Ver más", artist_path(artist), class: "btn btn-primary  btn-block" %></p>
        </div>
      </div>
    </div>
  <% end %>
</div>
</div>
